# 引入头文件目录
INCLUDE_DIRECTORIES(${PROJECT_SOURCE_DIR}/src)

# 引入动态链接库
LINK_DIRECTORIES(${LIBRARY_OUTPUT_PATH})


AUX_SOURCE_DIRECTORY(. SRC_LIST)
ADD_EXECUTABLE(test ${SRC_LIST})

TARGET_LINK_LIBRARIES(test LibtorchAssist)
